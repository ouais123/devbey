import 'package:flutter/material.dart';

class SearchLocationSuggestions extends StatelessWidget {
  const SearchLocationSuggestions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
